[build]
providers = ["python", "node"]
install = "cd backend && pip install -r requirements.txt && cd ../frontend && npm install"

[start]
cmd = "gunicorn --chdir backend main:app -w 4 -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
