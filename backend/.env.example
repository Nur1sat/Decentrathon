# Optional: PostgreSQL. If omitted, backend falls back to local SQLite
# DATABASE_URL=postgresql://postgres:password@localhost:5432/invision

# OpenAI API key (platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...
